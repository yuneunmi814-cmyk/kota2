# 독립 코드 리뷰 · 2026-09-05

첫 검토 기준: df7f6c2…4f44855. Standards와 Spec을 별도 검토자가 대조했다. 운영 DB나 배포 성공을 검증한 보고서는 아니다.

## Standards

명시적 저장소 규칙 위반은 발견하지 못했다. 공식 도메인, externalId, trailingSlash, Supabase 데이터 출처를 유지하며 새 CI는 검증용이다. SQL은 초안, 후기 전환은 기본 OFF이다.

판단적 개선점 2건(병합 차단 사유 아님):

1. Duplicated Code: review-identity.ts와 reviews.ts의 출처→UID 조회가 중복된다. 상세에서 후기와 평점을 함께 읽을 때 매핑 조회도 중복되므로 공통함수/요청단위 캐시를 검토할 수 있다. 현재 데이터 오류를 의미하지 않는다.
2. Shotgun Surgery / Duplicated Code: corrections.ts와 festivals.ts가 정정 필드 목록을 각각 가진다. 지금 필드는 일치하지만 새 필드를 추가할 때 함께 갱신해야 한다. 공유 목록 또는 동등성 테스트로 보완할 수 있다.

## Spec

첫 검토에서 P1 1건 발견: 대표 출처 ID가 변경되면 웹 공식 정정이 누락될 수 있다. 파이프라인은 sourceIds를 사용하지만 웹 DB 변환은 대표 ID만 제공한다.

승인 요구: “정정은 웹 실시간 병합 후에도 유지한다”, “공식 정정된 날짜/주소는 카드와 상세에서 동일하게 보인다.”

mock 조건: 대표 manual:representative / 옛 원천 tourapi:old, DB09-15 / live09-18 / 공식정정09-15 → 당시 카드·상세09-18. 기존 테스트는 통과하므로 새 대표변경 회귀가 필요하다. 순위용 정정도 같은 연결을 보장해야 한다.

상태: **f1738a2에서 코드·mock 수준 해소 확인**. 독립 Spec 검토자가 관련 쿼리 테스트 12개를 다시 실행해 모두 통과했다. 옛 출처→UID→현재 출처 연결을 카드·상세·preload·순위에 공급한다. 페이지네이션·TTL·테이블 부재/일반 장애 구분도 검증했다. 운영 매핑의 정확성·DB SQL/RLS 검증 완료를 뜻하지 않는다.

Standards 후속 검토에서도 f1738a2 추가 변경의 명시규칙 위반·중요한 냄새는 발견되지 않았다. 기존 판단성 개선점 2건은 남기되 병합 차단 사유가 아니다.

요약: Standards 명시규칙 위반0·판단적 제안2. Spec 최초 P1 1건→수정 후 잔존0. 두 축의 발견을 합쳐 우선순위를 다시 매기지 않았다.
