# 2026-09-02 KOTA 회의 후속 작업

기준 시각: 2026-09-02 22:43 KST

## 회의에서 확인된 남은 일

- 운영 리뷰·행동 로그를 읽기 전용으로 점검
- 과거·현재 축제 ID의 충돌·누락·중복 방지
- DB에 아직 없는 신규 축제도 상세 URL이 열리는지 시나리오 테스트
- 목록의 필터·정렬 규칙을 화면에서 분리
- 목록 URL·스크롤·위치 상태와 상세 페이지 역할 분리는 이후 단계

> 담당자와 기한은 녹취에서 확정되지 않았다. 화자 1이 작업을 이어가겠다고 말했지만 실명 매칭은 미확인이다.

## 운영 DB 읽기 전용 점검 결과

`pipeline/src/audit-production.ts`로 `SELECT/GET`만 실행했다. 리뷰 내용, 검색어, 방문자 ID는 출력하지 않았다.

| 항목 | 확인 결과 |
|---|---:|
| 현재 축제 | 581행 |
| 번역 | 1,743행 |
| 축제 사진 | 1,463행 |
| 리뷰 | 0행 |
| 저장·조회수·제보·배너 | 모두 0행 |
| 행동 로그 | 707행 |
| 현재 축제와 연결이 끊긴 행동 로그 | **21행 / 9개 과거 ID** |

확인된 연결 끊김 중 JIMFF, 수원 국가유산야행, 자라섬재즈는 대표 출처 ID가 바뀌면서 생긴 사례다. 다만 이미 종료된 행사가 현재 목록에서 빠져 끊긴 것도 섞여 있어, 9개를 모두 자동으로 현재 축제에 합치면 안 된다.

### 연결 확정 가능

| 과거 ID | 현재 ID | 근거 |
|---|---|---|
| `manual:jimff-2026` | `stdfest:제22회제천국제음악영화제-2026-09-03` | 기존 별칭 테스트·동일 행사 |
| `kfes:1916616` | `tourapi:1916616` | 같은 공사 CMS content ID |
| `manual:jarasum-jazz-2026` | `stdfest:2026년재즈페스티벌in가평-2026-10-09` | 동일 회차·이름·기간 |

### 사람 확인 후 연결

- 거제맥주축제: 과거 8월 일정과 현재 9월 일정이 같은 회차의 일정 수정인지 확인 필요
- 홍성남당항 대하축제: 과거 시작일과 현재 기간 차이 확인 필요
- 수원화성 미디어아트·동대문구 맥주축제: 이름은 일치하지만 날짜 변경 근거 확인 필요
- 백양사 산사음악회·보은 회인 국가유산야행: 현재 목록에 대응 행이 없어 과거 행사로 보존

## 이번에 완료한 코드 작업

1. `listFestivalSummaries()`를 추가해 목록·달력·테마가 상세 소개·프로그램·부스·사진 원문을 DB에서 가져오지 않게 했다.
2. 요약 조회에도 기존 TourAPI·표준데이터·구석구석 실시간 보정을 그대로 적용했다.
3. 필터·날짜순·거리순·인기순 규칙을 `web/lib/list-rules.ts`로 분리했다.
4. 필터·정렬 회귀 테스트 3개를 추가했다.
5. 반복 사용할 운영 DB 읽기 전용 점검 명령 `npm run audit:db`를 추가했다.

## 검증

- 자동 테스트: 13개 통과
- TypeScript 검사: 통과
- 전체 빌드: 2,379페이지 통과
- DB에 아직 없는 신규 축제 `tourapi:3343694`: 로컬 상세 URL 200
- 과거 JIMFF URL: 로컬 308 이동
- 운영 DB: 변경 없음

## 다음 작업

1. 9개 과거 ID의 확정·보존 분류를 바탕으로 영속 ID 스키마·검증·롤백 SQL 초안 작성
2. 파이프라인이 병합에 참여한 모든 출처 ID를 저장하고 기존 영속 ID를 재사용하도록 수정
3. 신규 영속 ID 증가량을 한 주 관찰한 후 웹 별칭 조회를 DB 기반으로 전환
4. 목록 URL·스크롤·위치 상태 분리와 상세 페이지 역할 분리

## 회의에서 나왔지만 아직 결정되지 않은 사항

- 팀원의 GitHub 브랜치 생성·PR 권한 부여 여부
- 상세 화면에 주변 숙소·식당을 보여 줄지
- Search Console 또는 별도 분석 도구로 유입 경로·국가별 방문을 어디까지 볼지
